// always import React and the other components that the pages will reference or use.
import React from 'react'

//stateless with no props passed
const NotFound = () => (
  <div>
    <h2>Not Found !!!!!!!!</h2>
  </div>
);

// remember to export the prop
export default NotFound;
